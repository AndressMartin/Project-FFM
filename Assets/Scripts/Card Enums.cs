using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CardEnums
{
    public enum Orientation { Battle, Exploration};
    public enum Type { Action, Minion, Relic};
    public enum Origin { Folk, Unique, Neutral};
    public enum Faction { Minos, Humans, Elves, Undead, Monsters};
    public enum Rarity { common, rare, exotic};

}
